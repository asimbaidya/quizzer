# Screenshots

Drop PNG captures here so they render in the root `README.md`. Suggested shots
(match the filenames referenced in the README):

- `student-quiz.png` — a student taking a quiz with graded feedback
- `teacher-author.png` — the "Add question" dialog
- `teacher-progress.png` — the per-student progress table

To capture: run the app (see the root README), sign in for each role, and grab
the relevant screen. Keep them ~1280px wide for crisp rendering.
